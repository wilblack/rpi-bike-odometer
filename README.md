## To Run

```python server.py```

Then you can navigate to `localhost:8888/app/index.html` in a broswer is it should show up.

To stop the server use `kill <PID>` where PID is the server's process ID gotten by `ps aux | grep server.py`

